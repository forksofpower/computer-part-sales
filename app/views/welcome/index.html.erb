<h1>Welcome!</h1>

<% if logged_in? %>
    <h3>You are logged in, <%= current_user.username %></h3>
<% else %>
    <%= button_to 'Login', login_path %>
    <%= button_to 'Sign Up', new_user_path, method: :get %>
<% end %>